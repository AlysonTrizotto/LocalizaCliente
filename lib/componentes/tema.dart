import 'package:flutter/material.dart';

final localizaTema = ThemeData(
        primaryColor: Colors.blueGrey[400],
        appBarTheme: AppBarTheme(
          backgroundColor: Color(0xFF101427), //use your hex code here
        ),
      );